# Test fixtures for the `.milk` parser (PRD M1)

These 8 files are real test fixtures copied verbatim from projectM's own
`presets/tests/` corpus (github.com/projectM-visualizer/projectm, LGPL-2.1),
used here only to verify `idMilkPreset`/`vis_milkLoad` against real preset
syntax during development -- not shipped as "content" in the ZGE/MilkDrop
sense described in the PRD.

Test with: `vis_milkLoad presets/milk/102-per_frame3.milk` (etc).

See `plans/PRD-zge-milkdrop-visualizer.md` Appendix C and
`plans/PRD-implementation-status.md` M1 for the full preset-pack sourcing
plan once real content packs get ingested.
